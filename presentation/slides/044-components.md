---
duration: 12
marker: Key Components
---

![[shared/components.md]]

---

`Lively::Assets` serves files straight from `public/` — CSS, JavaScript, images, fonts. Drop something in the folder and it's available immediately, no configuration required.

```javascript
slide.find("li").builder({group: "comp", effect: "fade"}).show(5)
```
